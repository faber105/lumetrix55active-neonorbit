import { BarChart3, Flame, Percent, Sigma } from 'lucide-react';
import { money } from '../utils/format';
export default function StatsOverview({stats}){const items=[['Сессий',stats?.total_sessions||0,BarChart3],['Winrate',`${stats?.winrate||0}%`,Percent],['Сделок',stats?.total_trades||0,Sigma],['Лучшая серия',stats?.best_streak||0,Flame]];return <section className="grid grid-cols-2 gap-3 px-4 py-4">{items.map(([label,value,Icon])=><div key={label} className="rounded-lg border border-terminal-border bg-terminal-card p-3"><Icon size={17} className="text-terminal-green"/><div className="mono mt-2 text-xl font-bold">{value}</div><div className="mt-1 text-xs text-terminal-muted">{label}</div></div>)}<div className="col-span-2 rounded-lg border border-terminal-border bg-terminal-card p-3"><div className="text-xs text-terminal-muted">Общий P&L</div><div className={`mono mt-1 text-2xl font-bold ${Number(stats?.total_pnl||0)>=0?'text-terminal-green':'text-terminal-red'}`}>{money(stats?.total_pnl)}</div></div></section>}
