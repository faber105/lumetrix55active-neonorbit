import { LockKeyhole } from 'lucide-react';
import { useEffect, useState } from 'react';
import { api, ensureAuth } from '../api/client';
import SubscriptionPlans from './SubscriptionPlans';
export default function SubscriptionGate({children}){const [status,setStatus]=useState(null);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{let active=true;(async()=>{setLoading(true);try{await ensureAuth();const {data}=await api.get('/subscription/status');if(active){setStatus(data);setError('')}}catch(err){if(active)setError(err.response?.data?.detail||err.message)}finally{if(active)setLoading(false)}})();return()=>{active=false}},[]);if(loading)return <div className="px-4 py-5"><div className="rounded-lg border border-terminal-border bg-terminal-card p-4 text-terminal-muted">Проверяю подписку...</div></div>;if(status?.is_active)return children;return <div className="pb-24"><section className="border-b border-terminal-border px-4 py-5"><div className="grid h-12 w-12 place-items-center rounded-lg bg-terminal-green/15 text-terminal-green"><LockKeyhole size={24}/></div><h1 className="mt-4 text-2xl font-extrabold">Доступ закрыт</h1><p className="mt-2 text-sm leading-6 text-terminal-muted">Сигналы, сессии и статистика доступны после покупки подписки.</p></section><main className="space-y-4 px-4 py-4">{error&&<div className="rounded-lg border border-terminal-red/50 bg-terminal-red/10 p-3 text-sm text-terminal-red">{String(error)}</div>}<SubscriptionPlans/></main></div>}
